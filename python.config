#!/bin/bash

DIR="$( cd "$( dirname "$BASH_SOURCE" )" && pwd -P )"

# references
. ${DIR}/functions

if [ $# != 1 ]; then
	msg "Format error"
	exit 1
else
	version=$1
	if [ ${version} < 3 ]; then
		version=2.7
		python_def ${version}
	else
		python3_def ${version}
	fi	
fi
